{
  "name": "workshop3d-windows-browser-publisher",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension + local bridge for WorkShop3D Publisher",
  "scripts": {
    "install:bridge": "node scripts/install-bridge.js",
    "build:extension": "node scripts/build-extension.js",
    "build": "npm run install:bridge && npm run build:extension",
    "start": "node bridge/src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "chokidar": "^4.0.1",
    "express": "^4.21.2",
    "nanoid": "^5.1.5",
    "express-rate-limit": "^7.4.1"
  }
}
