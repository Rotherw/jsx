# Local configuration with real paths / choices (never commit)
config/config.yaml

# Runtime working data, state and generated artefacts
work/
*.state.json
state.json

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets
.env
